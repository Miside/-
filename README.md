# Website Starter

这是一个 `Next.js` 网站起步项目，已经包含：

- 展示型首页
- 联系表单
- 后端接口 `/api/contact`
- Supabase 留言保存
- 简单留言后台 `/admin/messages`

## 本地启动

```bash
npm install
npm run dev
```

启动后打开：

```text
http://localhost:3000
```

## Supabase 数据库

在 Supabase 的 SQL Editor 里执行：

```sql
create table public.contact_messages (
  id bigint generated by default as identity primary key,
  name text not null,
  email text not null,
  message text not null,
  created_at timestamptz not null default now()
);

alter table public.contact_messages enable row level security;
```

不需要添加公开访问策略。网站后端会使用服务端环境变量写入数据库。

## Vercel 环境变量

在 Vercel 项目里打开：

```text
Settings -> Environment Variables
```

添加：

```text
SUPABASE_URL=你的 Supabase Project URL
SUPABASE_SERVICE_ROLE_KEY=你的 Supabase service_role key
ADMIN_TOKEN=你自己设置的后台口令
```

保存后重新部署一次。

## 使用方式

联系表单提交后，会写入 `contact_messages` 表。

后台查看地址：

```text
https://你的域名/admin/messages?token=你的后台口令
```

## 常改文件

- `app/page.tsx`: 首页内容
- `app/contact-form.tsx`: 联系表单
- `app/api/contact/route.ts`: 后端提交接口
- `app/admin/messages/page.tsx`: 留言后台
- `app/globals.css`: 全局样式
